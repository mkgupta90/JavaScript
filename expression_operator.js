// 1 : Assignment Operator

/*
// An assignment operator assign a value to its left operand
// the simple assignment operator is equal to (=)
var x = 5;
var y = 10;

//console.log("is both the x and y are equal to or not " + x == y); // Wrong Value
console.log(`is both the x and y are equal to or not : ${ x == y} ` ); 
*/

//------------------------------------------------------------------

// 2: Arithmetic operator
// Arithmetic opeartor takes nu,erical values

/*
console.log(3 + 3);  // addition
console.log(3 - 3);  // Substration
console.log(3 * 3);  // multiplication
console.log(3 / 3);  // division
console.log(3 % 3);  // modulus or reminder
console.log(3 ** 3);  // Exponential // 3 * 3 * 3 // 3 to the power (exponent) 3
console.log(3 ** 4);  // 3*3*3*3
*/

//---------------------------------------------------------------------

// 3: Comparison opeartor
// A comparison opeartor compare its opearnd and return a logical value on whether the comparison is true

/*
var a = 30;
var b = 10;

// Equal (==)
console.log(a == b);

// Not Equal (!=)
console.log(a != b);

//Greater than (>)
console.log( a > b);

// less than (<)
console.log( a < b);

// Greater than or Equal to (>=)
console.log( a >= b);

// Less than or Equal to
console.log(a <= b);
*/

//----------------------------------------
// 4: Logical operator
// Logical operators typically used with Boolean value. When they are return Boolean Value

/*
var a = 30;
var b = -20

// Logical AND (&&)
// AND opeartor for a set of opearnds is true if and only if all of its expressions are true
console.log(a > b && b > -50 && b < 0);

// Logical OR (||)
//AND opeartor for a set of opearnds is true if and only if one or more of its expressions are true
console.log( a > b || b > 0 || b < -50);

// Logical NOT (!)
// The logical NOT(!) oprator takes truth to falsity and vice versa
console.log(!((a > 0) || (b < 0)));
console.log(!true);
console.log(!false);
*/

//--------------------------------------------------------------------------


// 5 : String Concatenation opeartor
/*
 console.log("Munna kumar");
 console.log("Munna " + "Kumar");

var myName = "Munna";

console.log(myName + " Kumar");
console.log(myName + " Gupta");
console.log(myName + " Kumar " + "Gupta") ;
*/


//---------------------------------------------------------------------------------------------------

// 6: Conditional Opeartor (Ternary)









